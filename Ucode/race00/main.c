#include <stdio.h>

void race00(int map_length, int map_width, int one_y, int one_x);

int main(void)
{
    int map_length = 5;
    int map_width = 4;
    int one_y = 1;
    int one_x = 1;
    race00(map_length, map_width,one_y,one_x);
    return 0;
}
